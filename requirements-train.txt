# Training-only dependencies for the Vertex AI prebuilt-container run.
# The prebuilt TF container already provides tensorflow/keras/numpy, so this is
# the slim extra set (no gradio/pytest; opencv is headless for the VM).
# setup.py's install_requires is the source of truth for the packaged sdist;
# this file mirrors it for local venv installs / documentation.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
opencv-python-headless>=4.8
Pillow>=10.0
PyYAML>=6.0.2
google-cloud-storage>=2.18
matplotlib>=3.7

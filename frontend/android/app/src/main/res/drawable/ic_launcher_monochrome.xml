<?xml version="1.0" encoding="utf-8"?>
<!-- Themed-icon variant: the gradient bitmap has no usable alpha, so the
     single-color line art stands in for it. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_pulse_search"
    android:inset="26%" />

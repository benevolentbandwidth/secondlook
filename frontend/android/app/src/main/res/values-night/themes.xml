<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark counterpart, so launching in dark mode does not flash white. -->
    <style name="Theme.SecondLook" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
